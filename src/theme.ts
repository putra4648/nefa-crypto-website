import { createTheme } from '@mui/material';

const mainTheme: import('@mui/material').Theme = createTheme({
  palette: {
    primary: {
      main: '#ffff',
    },
    secondary: {
      main: '#468EF9',
    },
  },
});

export default mainTheme;
